--// Freeze Pets GUI (Grow a Garden)
-- ON = tất cả pet đứng im, pet spawn mới cũng bị đóng băng
-- OFF = pet hoạt động lại bình thường

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

-- GUI
local ScreenGui = Instance.new("ScreenGui", game.CoreGui)
local ToggleButton = Instance.new("TextButton", ScreenGui)
ToggleButton.Size = UDim2.new(0, 180, 0, 50)
ToggleButton.Position = UDim2.new(0.05, 0, 0.2, 0)
ToggleButton.Text = "🔴 Freeze Pets: OFF"
ToggleButton.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
ToggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleButton.Font = Enum.Font.SourceSansBold
ToggleButton.TextSize = 20
ToggleButton.Draggable = true

-- Trạng thái
local frozen = false

-- Hàm xử lý đóng băng
local function setPetsFrozen(state)
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("Model") and string.find(obj.Name:lower(), "pet") then
            for _, part in ipairs(obj:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.Anchored = state
                end
            end
        end
    end
end

-- Theo dõi pet mới spawn
workspace.DescendantAdded:Connect(function(obj)
    if frozen and obj:IsA("BasePart") and obj.Parent and obj.Parent:IsA("Model") then
        if string.find(obj.Parent.Name:lower(), "pet") then
            task.wait(0.1) -- chờ pet spawn đầy đủ
            obj.Anchored = true
        end
    end
end)

-- Toggle button
ToggleButton.MouseButton1Click:Connect(function()
    frozen = not frozen
    if frozen then
        ToggleButton.Text = "🟢 Freeze Pets: ON"
        ToggleButton.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
        setPetsFrozen(true)
    else
        ToggleButton.Text = "🔴 Freeze Pets: OFF"
        ToggleButton.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
        setPetsFrozen(false)
    end
end)
